import { StyleContentDirective } from './style-content.directive';

describe('StyleContentDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleContentDirective();
    expect(directive).toBeTruthy();
  });
});
